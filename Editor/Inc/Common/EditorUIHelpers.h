#pragma once

// Expose the shared inspector controls through the common editor include path while
// the original header remains available to existing component editors.
#include "Common/AssetEditorLayout.h"
#include "Inspector/ComponentEditorHelpers.h"
